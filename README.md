# SelectStart Front

Cliente 3D do MVP SelectStart. O navegador nunca recebe a chave GateGuard: login e situação financeira são validados pelo backend.

## Desenvolvimento

```bash
npm install
npm run dev
```

Configure `VITE_API_URL` conforme `.env.example`. A cidade instancia mil casas; cada conta recebe um número persistente pelo backend.
